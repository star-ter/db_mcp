[project]
name = "db-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=2.14.1",
    "mcp",
    "psycopg[binary]",
    "python-dotenv",
]
